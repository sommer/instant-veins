d-i apt-setup/use_mirror boolean true
d-i clock-setup/utc boolean true
d-i finish-install/reboot_in_progress note
d-i grub-installer/bootdev string /dev/sda
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i mirror/country string manual
d-i mirror/http/directory string /debian
d-i mirror/http/hostname string httpredir.debian.org
d-i mirror/http/proxy string
d-i partman-auto/choose_recipe select myroot
d-i partman-auto/disk string /dev/sda
d-i partman-auto/expert_recipe string myroot :: 1000 50 -1 ext4 $primary{ } $bootable{ } method{ format } format{ } use_filesystem{ } filesystem{ ext4 } mountpoint{ / } .
d-i partman-auto/method string regular
d-i partman-basicfilesystems/no_swap boolean false
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-lvm/device_remove_lvm boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true
d-i partman/confirm_write_new_label boolean true
d-i passwd/root-login boolean false
d-i passwd/root-password password veins
d-i passwd/root-password-again password veins
d-i passwd/user-fullname string veins
d-i passwd/user-password password veins
d-i passwd/user-password-again password veins
d-i passwd/user-uid string 1000
d-i passwd/username string veins
d-i pkgsel/include string sudo openssh-server
d-i pkgsel/install-language-support boolean false
d-i pkgsel/upgrade select full-upgrade
d-i time/zone string UTC
apt-cdrom-setup apt-setup/cdrom/set-first boolean false
apt-mirror-setup apt-setup/use_mirror boolean true
choose-mirror-bin mirror/http/proxy string
popularity-contest popularity-contest/participate boolean false
tasksel tasksel/first multiselect standard
